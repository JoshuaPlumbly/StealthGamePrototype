using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
namespace Assets.Code.FSM
{
    [CreateAssetMenu(menuName = "Direction/Decision/DecToSpawnBackup")]
    public class DecToSpawnBackup : DirectorDecision
    {
        public override bool Decide(AgentFSM agent)
        {


            if (targetDistance >= agent.AgentStats.minShootDist && targetDistance <= agent.AgentStats.maxShootDist)
                return true;
            else
                return false;
        }
    }
}*/